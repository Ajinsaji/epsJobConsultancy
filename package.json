{
  "name": "eps-job-consultancy-platform",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -k -n client,server -c cyan,magenta \"npm run client\" \"npm run server\"",

    "client": "npm --prefix client run dev",
    "server": "npm --prefix server run dev",
    "install-all": "npm install --prefix client && npm install --prefix server && npm install",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}


